import { Text } from "react-native";
import { styles } from "../utils/Css";

const MainComponent = () => {
  return <Text style={styles.heading}>Welcome to React Native</Text>;
};

export default MainComponent;